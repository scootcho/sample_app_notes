<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>  <!-- the followed_id column in the table needs to be send to the controller -->
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>